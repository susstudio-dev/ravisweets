# Postgres (local dev via docker-compose.yml at repo root)
DATABASE_URL=postgres://medusa:medusa@localhost:5432/ravisweets

# Redis (local dev via docker-compose.yml at repo root)
REDIS_URL=redis://localhost:6379

# Secrets — replace in staging/production
JWT_SECRET=replace-me
COOKIE_SECRET=replace-me

# CORS
STORE_CORS=http://localhost:3000
ADMIN_CORS=http://localhost:7001
AUTH_CORS=http://localhost:3000,http://localhost:7001

# Payment provider keys — leave empty until KYC lands
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=

# Store info (for GST invoice generator)
STORE_GSTIN=
STORE_FSSAI=
STORE_NAME=Ravi Sweets
STORE_ADDRESS=Hyderabad, Telangana, India

# Transactional email
RESEND_API_KEY=

# Observability
SENTRY_DSN=
POSTHOG_API_KEY=
